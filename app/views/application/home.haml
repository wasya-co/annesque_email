
.application-home.flexwidth

  - if current_user
    %h5 Hello.
    %div.d-flex.pb-2
      - if defined? wco_email
        .pr-3= link_to 'email', '/email'
      - if defined? wco_hosting
        .pr-3= link_to 'hosting', '/hosting'
      - if defined? iro
        .pr-3= link_to 'trading', '/trading'


  - else
    Not logged in.

  User: #{current_user.inspect}
